minimum_pre_commit_version: "4.0.0"

repos:
  - repo: local
    hooks:
      - id: validate-skills
        name: Validate skills
        entry: pixi
        args: [run, validate-skills, --]
        language: system
        files: ^skills/
        pass_filenames: true
