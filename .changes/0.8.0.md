## 0.8.0 - 2026-05-30
### Added
* Add a committed `.claude/settings.json` registering the `claude-plugins-official` marketplace and enabling the `skill-creator`, `plugin-dev`, and `superpowers` plugins (#110)
* Set the default model to Opus with always-on `xhigh` thinking, and `sonnet` for subagents, via the committed `.claude/settings.json` (#110)
### Changed
* Overhaul and rename the `cc-hooks` skill to `cc-hook` — rewritten against current Claude Code docs, now user-invocable as `/cc-hook` (#109)
### Removed
* Remove the `gemini-cli` skill and its artifacts (`.gemini/`, `GEMINI.md`, Speckit Gemini integration) after Gemini CLI was discontinued (#101)
