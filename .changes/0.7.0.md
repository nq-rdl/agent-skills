## 0.7.0 - 2026-05-12
### Added
* Adds new `send-pr` skill that delivers a single command to commit, push, raise a PR, and request review
* Document Gemini CLI reasoning effort / thinking budget / thinking level configuration via `modelConfigs` in `settings.json` in the `gemini-cli` skill (#99)
